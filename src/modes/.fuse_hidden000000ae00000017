import React from 'react';
import { motion } from 'framer-motion';

/**
 * PulseMode (Teaser / Coming Soon)
 * Deliberately scaled back for this sprint to focus on commerce.
 * Leaves the background transparent so the UnifiedGlobe underneath remains fully visible.
 */
export default function PulseMode() {
  return null;
}

