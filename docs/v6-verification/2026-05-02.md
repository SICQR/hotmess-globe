# HOTMESS v6 Verification Report — 2026-05-02

**Generated:** 2026-05-02 ~03:35 UTC  
**Production SHA:** `7f79cffe36c9` — "fix: unescape template literals in notifications/process WhatsApp handler"  
**Vercel deployment:** `dpl_A14g1hAwGgZ49w6xSNj26HWw8fH2` — **READY** ✅  
**Supabase project:** `rfoftonnlwudilafhfkl` (eu-west-2)

---

## Part 1 — Four Targeted Fixes

### Fix 1 — Stripe Webhook Hardening ✅

**Branch:** `fix/stripe-webhook-hardening` → merged to `main` as `ffa7f12438dd`

**1a. Removed `SUPABASE_ANON_KEY` fallback** ✅  
Before: `process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_ANON_KEY`  
After: hard guard that logs error if key missing.  
Risk eliminated: webhook was silently operating with anon-level permissions.

**1b. Idempotency table** ✅  
Migration `stripe_webhook_idempotency_table` applied. `processed_webhook_sessions` table created with `stripe_event_id` PK. RLS enabled. Webhook checks for duplicate event before processing.  
Verified: `SELECT COUNT(*) FROM processed_webhook_sessions` → 0 (new table).

**1c. New event handlers** ✅  
- `customer.subscription.deleted` → sets `subscription_status = 'canceled'` on profiles  
- `customer.subscription.updated` → logs `past_due` to `stripe_events_log`  
- `invoice.payment_failed` → logs attempt count to `stripe_events_log`

---

### Fix 2 — `safety_events` RLS Policies ✅

Migration `safety_events_rls_policies` applied.  
Before: RLS on, zero policies → all authenticated writes blocked.  
After: `users_insert_own_safety_events` (INSERT) + `users_read_own_safety_events` (SELECT), both scoped to `auth.uid() = user_id`. UPDATE/DELETE blocked by design.

---

### Fix 3 — ChatThread `chat_messages` → `messages` ✅

`src/components/messaging/ChatThread.jsx` — merged to main via `ffa7f12438dd`.  
- All `supabase.from('chat_messages')` → `supabase.from('messages')`  
- Malformed `.eq({thread_id: thread.id}, ...)` → `.eq('thread_id', thread.id).order('created_at', { ascending: false }).range(...)`  
- Realtime subscription: `table: 'Message'` → `table: 'messages'`  
- Added `if (msgErr) throw msgErr` error propagation  

Zero `chat_messages` references remain.

---

### Fix 4 — `trusted_contacts` Orphans ⚠️ NEEDS PHIL DECISION

2 rows with `user_id IS NULL` out of 3 total:

| id | name | phone | created |
|----|------|-------|---------|
| `f565704e` | glen | +447444302409 | 2026-03-27 |
| `929e5c2e` | Phil | 07444 302409 | 2026-03-31 |

Both added before the signup trigger fix (`6caef7e5`). Same phone in two formats — likely same person. **Need Phil to confirm delete or link to a real auth.uid() before NOT NULL constraint can be applied.**

---

## Part 2 — Verification Pass

### 2A — Stripe End-to-End 🔵 MANUAL REQUIRED

Cannot execute headlessly. Test with card `4242 4242 4242 4242`, any future expiry.  
Confirm: `memberships` row updates + `processed_webhook_sessions` gets a row.

### 2B — Care Surfaces 🔵 MANUAL REQUIRED

Based on code audit:
- `api/safety/check-ins.js` — POST/GET/cron handlers implemented ✅
- Check-in cron every 2 minutes (vercel.json) ✅
- WhatsApp delivery in `api/notifications/process.js` ✅ (template literal bug fixed `7f79cffe`)
- Surfaces to test: Backup, Get Out/SOS, Cover (fake call), Land Time (check-in), Clean Exit

### 2C — Meet Flow

`meet_sessions`: 0 rows | `meet_outcomes`: 1 row (type: `found`)  
RLS confirmed on both tables. End-to-end test requires two authenticated users.

### 2D — F5M Onboarding ✅

`feature_flags.flag_key = 'v6_first_five_minutes'` → `enabled_globally = true` ✅  
Flipped globally this session. All new signups hit the F5M flow (9 screens).

### 2E — Globe + Beacons

| Metric | Value |
|--------|-------|
| Active beacons | 0 (03:00 UTC — expected) |
| Profiles with location | 12 / 50 |
| PostGIS | ✅ |

### 2F — Crons Health

| Cron | Schedule | Status |
|------|----------|--------|
| `/api/notifications/dispatch` | `*/5 * * * *` | ✅ 200, 192 runs, last ok 03:30 UTC |
| `/api/notifications/process` | `*/5 * * * *` | ✅ Fixed this session (`7f79cffe`) — was 500 on every hit |
| `/api/safety/check-ins` | `*/2 * * * *` | 🔵 New — no cron_runs logging yet |
| `/api/cron/data-retention` | `0 2 * * *` | ❌ 500 at 02:00 UTC (see below) |
| `/api/events/cron` | hourly | ⚠️ DEP0169 deprecation warning |

**data-retention root cause:** `NEXT_PUBLIC_SUPABASE_URL` is `undefined` in serverless context — `createClient(undefined, key)` throws before the try/catch. cron_runs record stuck in `running`.  
**Fix:** `process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL` on line 20 of `api/cron/data-retention.js`.

### 2G — RLS Audit

All 19 v6 tables have `rowsecurity = true` ✅

Notable findings:
- `safety_events` — policies added this session ✅
- `processed_webhook_sessions` — RLS on, 0 policies (service role bypasses RLS — acceptable)
- `trusted_contacts` — has an ALL `{public}` policy alongside authenticated ones ⚠️
- `blocks`, `saved_items`, `location_shares`, `user_active_boosts` — policies use `{public}` role, should be `{authenticated}` ⚠️ (low severity — `using` clauses still check `auth.uid()`)

### 2H — Storage + Google Maps

Storage buckets (9 total):

| Bucket | Public |
|--------|--------|
| records-audio | ✅ public — **bucket confirmed exists** |
| thread-attachments | 🔒 private ✅ |
| chat-uploads | ✅ public ⚠️ (should be restricted) |
| messmarket-images, records-covers, avatars, uploads, radio-assets, brand-assets | public |

Google Maps API key restriction — requires manual check in Google Cloud Console.

---

## Incidents Found & Fixed This Session

| Issue | Fix | Commit |
|-------|-----|--------|
| `notifications/process` 500 every 5min — 8 backticks + 4 `\${` escaped in WhatsApp handler | Unescaped | `7f79cffe36c9` |
| `safety_events` RLS on, zero policies — all writes blocked | Added INSERT + SELECT policies | DB migration |
| ChatThread querying nonexistent `chat_messages` table + malformed `.eq()` | Fixed to `messages` table | `ffa7f12438dd` |
| Stripe webhook anon key fallback + no idempotency + missing handlers | Hardened | `ffa7f12438dd` |
| Stripe webhook DEBUG signature bypass | Removed | `09141b625b` |

---

## Open Issues

| Priority | Issue |
|----------|-------|
| HIGH | `data-retention` cron 500 daily — 1-line env var fix needed |
| HIGH | `trusted_contacts` 2 orphan rows — Phil decision needed |
| MEDIUM | 11 `notification_outbox` rows stuck `failed` (from WhatsApp bug window) — manual retry or delete |
| MEDIUM | Google OAuth "Unable to exchange external code: 4/0A" — refresh Google client secret in Supabase Dashboard |
| LOW | RLS `{public}` role on 4 tables — should be `{authenticated}` |
| LOW | `chat-uploads` bucket public |
| LOW | `events/cron` DEP0169 warning |
| BLOCKED | WhatsApp E2E — Meta phone number verification pending |
| BLOCKED | Google Maps API key restriction — manual verify |
