# ⚠️ SECURITY WARNING: NEVER commit this file with actual values!
# This file is for documentation purposes only. Copy to .env.local with real values.
# .env and .env.local are gitignored for your protection.

# =============================================================================
# BASE44 CONFIGURATION (Required)
# =============================================================================
# Get these from your Base44 dashboard: https://base44.app
VITE_BASE44_APP_ID=your_app_id_here
VITE_BASE44_APP_BASE_URL=https://your-app.base44.app

# Base44 Legacy SDK Support (Optional)
# Set to 'true' if using legacy Base44 SDK imports
BASE44_LEGACY_SDK_IMPORTS=false

# =============================================================================
# MAPBOX CONFIGURATION (Required for map features)
# =============================================================================
# Get your access token from: https://account.mapbox.com/access-tokens/
# ⚠️ Use a PUBLIC token (restricted to specific URLs in production)
VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# =============================================================================
# SUPABASE CONFIGURATION (Required for real-time features)
# =============================================================================
# Get these from your Supabase project settings: https://app.supabase.com
# ⚠️ Use the ANON key (public key), NOT the service role key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ⚠️ CRITICAL: Service role keys must NEVER be exposed client-side!
# Backend/serverless functions should use environment variables WITHOUT the VITE_ prefix
# Never uncomment or add service role keys to this file

# =============================================================================
# STRIPE CONFIGURATION (Required for payment features)
# =============================================================================
# Get your publishable key from: https://dashboard.stripe.com/apikeys
# ⚠️ Use PUBLISHABLE key (pk_test_* or pk_live_*), NOT secret key
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key_here

# ⚠️ CRITICAL: Secret keys must NEVER be exposed client-side!
# Backend/serverless functions should use environment variables WITHOUT the VITE_ prefix
# Never uncomment or add secret keys to this file

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# =============================================================================
# DEVELOPMENT/DEBUG SETTINGS (Optional)
# =============================================================================
# NODE_ENV=development
# VITE_DEBUG_MODE=false

# =============================================================================
# ⚠️ SECURITY REMINDERS
# =============================================================================
# 
# ✅ DO:
# - Copy this file to .env.local and add your real values
# - Use VITE_ prefix ONLY for values that MUST be client-side
# - Keep API secret keys server-side (no VITE_ prefix)
# - Use restricted/scoped API keys when possible
# - Rotate keys immediately if accidentally committed
# 
# ❌ DON'T:
# - Commit .env or .env.local files
# - Put secret keys in client-side code (VITE_ variables)
# - Share keys in plaintext (Slack, email, etc.)
# - Use production keys in development
# - Hardcode any credentials in source code
#
# 📚 For more information, see SECURITY.md
# =============================================================================

